CREATE TABLE IF NOT EXISTS doginfo_test.Dog (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    age INT,
    imgURL VARCHAR(255),
    species VARCHAR(255),
    AdoptStatus VARCHAR(255),
    MedicalHistory VARCHAR(255),
    intro VARCHAR(255)
);
